kind: Secret
apiVersion: v1
metadata:
  name: datasource-credentials
stringData:
  PROMETHEUS_USERNAME: {{ .Values.prometheus.username }}
  PROMETHEUS_PASSWORD: {{ .Values.prometheus.password }}
type: Opaque