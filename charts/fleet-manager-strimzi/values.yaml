certs:
  kafka:
    cert:
    key:
oauth:
  clientId: 
  issuer: 
  jwksEndpoint:
  subClaim: sub
tenancy: Shared
tolerations: []
nodeAffinity:
domain:
strimzi:
  version: 0.45.0
kafka:
  version: 3.8.0
  diskSize: 100Gi
  listeners:
    external: 
      enabled: true
    internal: 
      enabled: true
  resources:
    requests:
      cpu:
      memory:
    limits:
      cpu:
      memory:
  tieredStorage:
    enabled: true # Set to true for backwards compatibility
    provider: S3
    cloud: aws
    bucketName: 
    endpoint: 
    aws:
      accessKey:
      secretKey:
      region:
kraft:
  diskSize: 50Gi
replicas: 3