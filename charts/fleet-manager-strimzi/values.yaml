certs:
  kafka:
    cert:
    key:
oauth:
  clientId: 
  issuer: 
  jwksEndpoint:
  subClaim: sub
tenancy: Shared
domain:
strimzi:
  version: 0.45.0
kafka:
  version: 3.8.0
  diskSize: 100Gi
  listeners:
    external: 
      enabled: true
    internal: 
      enabled: true
  tieredStorage:
    provider: S3
    cloud: aws
    bucketName: 
    endpoint: 
    aws:
      accessKey:
      secretKey:
      region:
kraft:
  diskSize: 50Gi
replicas: 3