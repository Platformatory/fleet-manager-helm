certs:
  ca:
    cert:
    key:
  kafka:
    cert:
    key:
  mds:
    public:
    keyPair:
oauth:
  clientId:
  clientSecret: 
  audience: 
  issuer: 
  jwksEndpoint:
  authorizeEndpoint:
  tokenEndpoint: 
  scope:  
  subClaim: sub
  groupsClaim: group
  refreshToken: true
tenancy: Shared
replicas: 3
domain: 
ingressClassName: 
cfk:
  initContainer:
    version:
  confluentPlatform:
    version: 
    kraft:
      diskSize: 50Gi
    kafka:
      diskSize: 100Gi
      listeners:
        internal:
          enabled: true
        external:
          enabled: true
    controlCenter:
      diskSize: 30Gi
  license:
  systemAdmin:
    name:
    type:
  tieredStorage:
    provider: S3
    bucketName: 
    aws:
      accessKey:
      secretKey:
      region: